class Grid{
  constructor(width,height,dx,dy,modX,modY){
    this.width = width;
    this.heigth = height;
    this.dx = dx || 20;
    this.dy = dy || 20;
    this.modX = modX || 100;
    this.modY = modY || 100;
  }

  draw(context){
    //
  }
}
